import Canvas from "./canvas";
import City from "../../game/city";

export default class CityCanvas extends Canvas {

    constructor() {
        super("city-canvas", 2);
    }

    show(city: City) {

    }
}