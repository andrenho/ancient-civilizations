name: Publish page
on:
  push:
    branches: [ master ]
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      # Build typescript
      - uses: iCrawl/action-tsc@v1

      
